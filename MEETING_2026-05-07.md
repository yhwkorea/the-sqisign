# MLLL 통합 진행 보고 — 2026-05-07 미팅

**작성**: 2026-05-05 · **브랜치**: `feat/mlll-pr{1..5}-*` · **저장소**: `github.com/yhwkorea/the-sqisign`
**기존 자료**: `PRESENTATION_KO.md` (전체판, 2026-04-29) — 이 문서는 그 이후 Phase 3 결과 갱신본.

---

## 1. 한 줄 요약 (먼저 말할 것)

> **"외부 감사로 거짓 표기가 발각된 후, CLAUDE.md HARD RULES 신설 → 47 commit을 5개 stacked PR로 분할 → ON/OFF 양쪽 빌드 18/18 + scheme L1/L3/L5 PASS. routing은 (1) `nm` symbol grep, (2) ON/OFF 데이터 byte 차이, (3) +0.4–2.5 % wall-clock overhead — **3중 증거**로 검증. ON `.rsp.MLLL`은 현재 HEAD 결정론적 출력으로 refresh 완료."**

핵심 지표 1줄:

| 빌드 | mlll | scheme | KAT | _mlll_gram routing | overhead |
|---|---|---|---|---|---|
| OFF (HNF) | 18/18 | L1/L3/L5 PASS | "PASSED" (LF normalize) | 미참조 (예상) | baseline |
| ON (MLLL) | 18/18 | L1/L3/L5 PASS | `.rsp.MLLL` refresh (commit `c25aa18`) | normeq.c.o ref + ON≠OFF byte | +0.4–2.5 % |

---

## 2. 발표 흐름 (15–20분 권장)

### 슬라이드 1: 사건 (3분)
**왜 이 작업이 시작됐나**

- 2026-04-29 외부 감사
- README 표기: "MLLL을 단독 기본 경로로 사용 / paper-strict 100%"
- 실제 빌드: `grep -rn '_mlll_gram\b' src/signature/ src/id2iso/ src/sqisign.c` = **빈 출력** (sign.c 어디에서도 MLLL을 호출하지 않음)
- 즉 README가 빌드 현실과 모순 → GitHub에 거짓 사실 push된 상태로 발견됨

**해석**: "구현 끝"의 의미가 분명히 정의되지 않음. **알고리즘 본체 작성** 끝났다고 **빌드 통합** 끝났다고 잘못 보고함.

### 슬라이드 2: 룰북 신설 (2분)
**CLAUDE.md HARD RULES (저장소 최상단, glob CLAUDE보다 우선)**

1. **§1.3 금지 표현** — "단독", "100%", "구현 완료", "NIST 통과"
2. **§2.2 범위 명시 의무** — 보고 시 두 단어 중 하나로 한정:
   - **알고리즘 한정** — 본체 + 단위 테스트만 검증
   - **빌드 통합** — 핫 패스에서 실제 호출 + round-trip 검증
3. **§2.3 README ↔ commit msg ↔ Claude 응답 일치** — 세 곳이 동일한 단어
4. **§3 Phase 분리 명시** — 5단계 acceptance criterion 표 (Phase 1 ~ 3-5)
5. **§4 거짓 발견 시** — rebase/amend 금지, 정정 commit으로 명시 추적

### 슬라이드 3: PR 분할 전략 (3분)
**왜 5개 stacked PR**

- 47 commits를 단일 PR로 push하면 review 불가능
- 논리적 의존 단계별로 분할: kernel → wrappers → applications → backend → routing

```
PR1 (main)              kernel: paper Alg 1 본체 (Pohst 1987 정수 GSO)
 └ PR2 (PR1)            Alg 2/3: lattice mul / lideal create (Gram 래퍼)
    ├ PR3 (PR2)         Alg 1/4: lideal_reduce_basis_mlll_gram + prime_norm_…
    └ PR4 (PR2)         fp backend: 4 backend selector + paper-strict mlll.c 재작성
       └ PR5 (PR4)      hot-path routing: SQISIGN_USE_MLLL_GRAM 매크로 alias + meta
```

**왜 PR3가 PR4 위가 아닌 PR2 위에 stack됐나**: 의존성 분기 — PR3 helper 본체는 PR4의 quaternion.h 선언을 logically 필요로 하는데, PR4 테스트는 PR3 테스트와 mlll_tests.c에서 충돌. 결정: PR3와 PR5 모두 helper 본체를 보유 (squash-on-merge로 해소).

### 슬라이드 4: 핵심 fix commit 두 개 (3분)
**정직 추적 (CLAUDE.md §4)**

#### Fix #1 — `772f3e3` (PR2): audit Bareiss swap 회귀
- audit commit `4c3a599`가 정수 GSO swap 경로를 NTL-style Bareiss로 교체
- production 입력에서 `assert(ibz_is_zero(&rem))` 실패 — `d[k] | tmp_a` invariant 위반
- → PR1의 안전 swap (`do_swap_dep` + `gram_swap` + `recompute_all_lam_d`) 복원
- HEAD에서 mask됐던 이유: PR5의 매크로 alias가 핫 패스를 `mlll_gram.c`로 routing해서 `mlll.c` swap 경로 미사용

#### Fix #2 — `29b6247` (PR4): orphan link bug
- `5ef9b76`가 `quat_test_alg4_mlll_gram_equivalence` 호출 추가했으나 함수 정의 누락
- 원본 `feat/mlll-ideal-operations` branch도 동일 dormant link bug 보유 (sqisign_test_mlll 빌드 시에만 surface)
- → orphan call 제거. 동일 의미는 `quat_test_lideal_create_gram_equivalence` (25 trials, HNF↔MLLL_GRAM lattice equality)로 커버

### 슬라이드 5: 핵심 아키텍처 — 매크로 alias routing (3분)
**왜 매크로 alias인가**

- 옵션 1 (직접 호출): `sign.c`에서 `quat_lideal_reduce_basis_mlll_gram(...)` 명시 호출 → sign.c 수정 필요
- 옵션 2 (런타임 dispatch): 함수 포인터 테이블 → 분기 비용 + 코드 복잡도
- **선택: 옵션 3 — 매크로 alias** (`src/quaternion/ref/generic/include/quaternion.h`)
  ```c
  #if defined(SQISIGN_USE_MLLL_GRAM) && !defined(SQISIGN_MLLL_GRAM_IMPL)
  #undef quat_lideal_create
  #undef quat_lideal_reduce_basis
  #undef quat_lideal_prime_norm_reduced_equivalent
  #undef quat_lideal_lideal_mul_reduced
  #define quat_lideal_create                        quat_lideal_create_mlll_gram
  #define quat_lideal_reduce_basis                  quat_lideal_reduce_basis_mlll_gram
  #define quat_lideal_prime_norm_reduced_equivalent quat_lideal_prime_norm_reduced_equivalent_mlll_gram
  #define quat_lideal_lideal_mul_reduced            quat_lideal_lideal_mul_reduced_mlll_gram
  #endif
  ```
  - sign.c / keygen.c / id2iso 핫 패스 **무수정** (preprocessor 단계에서 4개 함수 redirect)
  - `SQISIGN_MLLL_GRAM_IMPL` guard로 함수 본체 정의 파일 / HNF↔MLLL 비교 테스트는 원 이름 유지
  - HNF namespace 매크로 (sqisign_namespace.h의 per-level mangling)와의 충돌은 `#undef` 선행으로 해결 (commit `2e4d178`)

**검증 방법** (CLAUDE.md Phase 3-2 acceptance #2):
- ON/OFF 두 빌드의 `.o` 파일에 `nm | grep _mlll_gram` → ON 빌드만 `normeq.c.o` (id2iso 핫 패스)에 참조 ⇒ routing 실작동

### 슬라이드 6: 정밀도 백엔드 (2분)
**4 backend selector** (`mlll_fp_ops.h`)

| Kind | 표현 | 정밀도 | 비고 |
|---|---|---|---|
| 0 | `double` | 53b | L5 overflow 예상 (paper bound 1010b) |
| 1 | `mpf_t` (GMP) | 가변 (default 128b) | 정밀도 조정 가능 |
| 2 | `mpfr_t` | 가변 (default 1024b) | **현재 default** — correctly-rounded |
| 3 | `dpe_t` | double + int64 exp | fplll 스타일 |

**default 결정**: D2c (mpfr_t PREC=1024) — L5 paper bound 1010b를 **14b 마진**으로 커버.

**fp budget 연구 결과** (commit `c3e7b37`): production gram이 ~2555 bits 관측 → ON 빌드에서 fp path가 ibz_t fallback. **paper-strict 의도의 일부 회귀이지만 실증 데이터 기반 의도된 정정**.

### 슬라이드 7: 검증 결과 (3분)
**OFF (HNF default) + ON (`-DSQISIGN_USE_MLLL_GRAM=ON`) 양쪽 빌드 모두**

| 검증 | OFF | ON |
|---|---|---|
| `sqisign_test_mlll` 단위 | 18/18 PASS | 18/18 PASS |
| `sqisign_test_scheme_lvl{1,3,5}` | exit=0 | exit=0 |
| `sqisign_test_kat_lvl{1,3,5}` | "Known Answer Tests PASSED" | HNF .rsp byte-mismatch (수학적 필연) |
| compiled `.o` _mlll_gram 참조 | 미참조 | normeq.c.o ✅ |
| `PQCgenKAT_sign` 출력 (lvl1/3/5, same seed) | `.rsp` (LF 정규화 후 baseline 일치) | `.rsp.MLLL` (refresh 완료, ON ≠ OFF) |

**KAT byte-mismatch 설명** (질문 예상):
- HNF KAT은 HNF reduction 결과의 byte-exact snapshot
- MLLL은 다른 lattice representative (GL_4(ℤ) 동치류 내 다른 대표)를 선택
- → byte-exact 일치는 **수학적으로 불가능**. paper도 byte-compat 요구하지 않음.
- 회귀 추적은 OFF baseline (HNF KAT) + ON scheme test (signature validity)로 분리 검증.

### 슬라이드 8: 미완성 / 알려진 한계 (2분)
**정직 표기 (CLAUDE.md §1.1 README 의무 섹션)**

- **Phase 3-3 완료** (commit `c1028b3`, 2026-05-05): `apps/PQCgenKAT_sign.c`에 `USE_MLLL_GRAM` 분기 추가 → ON 빌드는 `.rsp.MLLL`, OFF 빌드는 `.rsp` 출력. 두 빌드 모두 100 iter 정상.
- **`KAT/*.rsp.MLLL` refresh 완료** (commit `c25aa18`, 2026-05-06): 현재 HEAD `c1028b3` ON 빌드 결정론적 출력으로 교체. byte-exact 일치 (`build_pr5_on` / `MLLL_FP_KIND=2` mpfr_t PREC=1024, 100 iter, LF 정규화 기준).
- **추가 acceptance 데이터** — 같은 seed에서 ON `.rsp.MLLL` ≠ OFF `.rsp` 확인 (lvl1/3/5 모두). 매크로 alias routing이 keypair/sign 출력 byte까지 영향을 미친다는 data-level 증거 (Phase 3-2 acceptance #2 nm grep + #3 make test에 더해).
- **Phase 3-5 부분 수행**: HNF↔MLLL_GRAM e2e 동치성은 두 빌드 scheme PASS로 self-consistency 확인. byte-level 동치는 위 KAT 설명대로 불가능.
- **fp paper-strict 회귀**: ON 빌드에서 ibz_t fallback (production lattice가 fp budget 초과). 추가 연구 필요.

### 슬라이드 9: 성능 측정 + 다음 단계 (2분)

**ON vs OFF wall-clock + memory** (PQCgenKAT_sign 100 iter, WSL2 Ubuntu, MLLL_FP_KIND=2 mpfr_t PREC=1024, BUILD_TYPE=ref Release, 2026-05-06):

| Level | OFF (HNF) | ON (MLLL) | time overhead | maxrss (ON/OFF) | mem overhead |
|---|---|---|---|---|---|
| lvl1 | 6.85 s | 6.88 s | **+0.4 %** | 4228 / 4112 KB | +2.8 % |
| lvl3 | 14.40 s | 14.51 s | **+0.8 %** | 7564 / 7432 KB | +1.8 % |
| lvl5 | 23.65 s | 24.25 s | **+2.5 %** | 6972 / 6900 KB | +1.0 % |

해석: routing 매크로 alias가 hot path에 진입함에도 (Phase 3-2 acceptance #2 참조), MLLL_GRAM 실행 비용은 lvl5 기준 +2.5 % 이내. paper-strict integer-rational arithmetic 가 정수 GSO 비용을 sign 전체에서 매우 낮은 비율로 유지함.

**남은 다음 단계**:

1. **Merge order**: PR1 → PR2 → PR3 → PR4 → PR5 (squash on merge for PR3/PR5 helper duplication)
2. ~~`KAT/*.rsp.MLLL` 갱신~~ — **완료** (commit `c25aa18`, 2026-05-06)
3. ~~Phase 5 cycle bench / mem 회귀~~ — **완료** (위 표, commit `d7af14a`)
4. **Phase 4 (선택)**: fp budget 확장 연구 (production lattice가 왜 2555b까지 가는지 정량 분석)
5. **CI 워크플로 `mlll-routing-check`** GitHub Actions 결과 확인 (push된 5 PR 모두)

---

## 3. 예상 질문 + 답변 준비

### Q1. "왜 단일 PR이 아니라 5개로 나눴나?"
A. 47 commit을 단일 PR로는 review 불가. 논리 의존 단계별로 분할 — kernel(추상)/wrappers(추상)/applications(연결)/backend(인프라)/routing(통합). 각 PR이 독립 검증 가능 (자체 단위 테스트 PASS).

### Q2. "거짓 표기는 어떻게 다시 안 일어나게 했나?"
A. CLAUDE.md HARD RULES — repository-level 규칙 + glob CLAUDE보다 우선. §2.1 자가 grep 의무, §2.2 범위 명시 두 단어 (알고리즘 한정 / 빌드 통합), §1.3 금지 표현 리스트, §4 정정 commit 추적 의무.

### Q3. "ON 빌드 KAT이 왜 실패하는데 PASS라고 하나?"
A. KAT은 HNF reduction 결과의 byte-exact snapshot. MLLL은 다른 lattice representative를 선택 → byte-exact 일치는 수학적으로 불가능. paper도 byte-compat 요구하지 않음. **회귀 추적은 분리**: OFF KAT (HNF baseline) + ON scheme test (signature validity).

### Q4. "fp backend가 왜 ibz_t로 fallback?"
A. fp budget 연구 (`bench_logs/p4_a_input_bits/`) — production gram이 ~2555 bits 관측. paper-strict 1010b 가정과 차이. 추가 연구 전까지 안전 fallback. paper bound 측정값 (L1=506b, L3=769b, L5=1016b)은 default 빌드 (USE_MLLL_GRAM=OFF) 의 보조 호출 측정.

### Q5. "PR3와 PR5에 helper 본체가 중복인 이유?"
A. 의존성 분기 — PR3 본체는 PR4 헤더 선언이 logically 필요하지만 PR4는 PR3 테스트와 충돌. PR3는 PR2 위 / PR4는 PR2 위 / PR5는 PR4 위. PR5에서 PR3의 helper 본체를 cherry-pick으로 가져옴 — squash-on-merge로 중복 해소 (review 시점만 중복).

### Q6. "routing이 정말 동작하는지 어떻게 증명?"
A. **컴파일된 `.o` 비교** (CLAUDE.md Phase 3-2 acceptance #2):
```bash
# OFF 빌드
nm build_pr5_off/...normeq.c.o | grep _mlll_gram   # 미참조
# ON 빌드  
nm build_pr5_on/...normeq.c.o  | grep _mlll_gram   # 참조 ✅
```
ON 빌드만 normeq.c (id2iso 핫 패스)에 `_mlll_gram` symbol 참조 생성 ⇒ preprocessor 단계의 매크로 alias가 실제로 hot path를 redirect.

### Q7. "ON 빌드 시간 회귀는?"
A. **lvl1 +0.4 %, lvl3 +0.8 %, lvl5 +2.5 %** (PQCgenKAT_sign 100 iter wall-clock, MLLL_FP_KIND=2 mpfr_t PREC=1024, WSL2 Ubuntu, 2026-05-06). 메모리는 +1.0 ~ +2.8 % 추가. Slide 9 표 참조.

---

## 4. 발표 시 절대 쓰지 말 단어 (CLAUDE.md §1.3)

- ❌ "단독 기본 경로로 사용"
- ❌ "paper-strict 100%"
- ❌ "다 구현 완료"
- ❌ "NIST 통과"

대신:

- ✅ "Phase 3-2 acceptance #2/#3 통과 (ON+OFF 양쪽 18/18 + scheme PASS)"
- ✅ "단위 테스트 18/18 PASS, scheme test 3 레벨 PASS, 매크로 alias routing 컴파일된 `.o` nm grep으로 검증"
- ✅ "알고리즘 한정 90% / 빌드 통합 70%" (Phase 3-3 미수행 인정)

---

## 5. 자료 위치

| 자료 | 경로 |
|---|---|
| 전체판 (배경/수학) | `PRESENTATION_KO.md` |
| 진행 갱신본 (이 파일) | `MEETING_2026-05-07.md` |
| 룰북 | `CLAUDE.md` |
| 원본 사건 기록 | `CLAUDE.md` §0 (저장소 최상단) |
| 검증 로그 | `~/.claude/verification_log.md` (전 43 entries, 2026-05-06 기준) |
| Phase plan | `src/quaternion/ref/generic/lll/PLAN_KO.md` |
| PR 5개 | https://github.com/yhwkorea/the-sqisign/pull/{1..5} |
